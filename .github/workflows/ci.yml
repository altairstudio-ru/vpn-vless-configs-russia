name: Auto Mirror (every 30 minutes)

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

permissions:
  contents: write    # дать GITHUB_TOKEN право коммитить

concurrency:
  group: auto-mirror
  cancel-in-progress: true

jobs:
  update:
# ... (остальное без изменений)

